/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // If you use next/image with remote URLs, add:
  // images: { domains: ['your-cdn.com'] },
};

module.exports = nextConfig;
